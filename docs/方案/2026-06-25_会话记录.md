# 会话记录 — 2026-06-25

> 本文件记录本次会话的所有关键决策和修改内容，供后续模式切换时读取。

---

## 一、本次完成的工作

### 1. Git 仓库切换
- **旧仓库：** `https://gitee.com/chenguanheng1988/novel.git`
- **新仓库：** `https://github.com/LAOYE1988/novel.git`
- 安装了 GitHub CLI（v2.95.0），认证账户 **LAOYE1988**
- 创建公开仓库 `novel`，推送 master 分支
- 后续 `/提交` / `/下载` / `/回滚` 均操作新仓库

### 2. 安全防护
- 添加 `.gitignore`：保护 `.env`、`__pycache__`、系统文件不被误提交
- 确认 `.env` 文件从未被 Git 追踪过，历史记录干净
- Git 安全机制：`git pull --ff-only` 防止反向覆盖

### 3. 快捷脚本（已提交但后续被删除）
- 曾创建 `提交.bat` / `下载.bat` / `回滚.bat`，后因项目规则通过 Skill 执行 Git 操作而被清理

### 4. 顶层规则重构（核心产出）

#### `project_rules.md` — 最终定稿结构

| 章节 | 内容 |
|:----:|:----:|
| 一 | 项目概览（技术栈、仓库地址） |
| 二 | 通用规则（沟通、文件安全、Git、启动、文档约束） |
| 三 | 三个模式是独立的 AI（核心认知） |
| 四 | 模式间信息传递（文件为主，口头为辅，不用 OpenViking） |
| 五 | 各模式行为规范（CODE 主力、WORK 辅助、DESIGN 辅助） |
| 六 | 关键约束（6 条安全底线） |

#### `脑图.md` — 最终定稿结构

| 章节 | 内容 |
|:----:|:----:|
| 脑图 A | 项目物理结构（目录树） |
| 脑图 B | 三模式关系（真实版，三个独立 AI） |
| 信息传递方式 | 文件传递流程图 |
| OpenViking 定位 | 只做检索，不做传递 |
| Git 版本控制 | 提交/下载/回滚流程图 |
| 数据流 | 编辑器→API→文件流 |

### 5. 三模式最终定位

```
CODE  ── 核心主力 ── 没有 WORK 和 DESIGN 也能独立干活
WORK  ── 辅助增强 ── 聊方案、输出 docs/方案/ 文档
DESIGN ─ 辅助增强 ── 只在需要美化时介入，不参与功能决策
```

---

## 二、关键决策记录

| 决策 | 结论 |
|:----|:----:|
| 仓库来源 | Gitee → GitHub |
| 文件保护 | `.gitignore` 已配置，.env 历史干净 |
| 防覆盖策略 | `git pull --ff-only`，先 commit 再 pull |
| 三模式协作 | 模式间对话不通，信息靠文件传递 |
| OpenViking 用途 | 仅做快速检索，不做信息传递 |
| WORK 输出位置 | `docs/方案/YYYY-MM-DD_主题.md` |

---

## 三、下一步建议

1. **进入 CODE 模式** — 开始做小说工具的功能开发
2. **或继续 WORK 模式** — 聊具体的功能规划方案
3. **启动工作台** — `python tools\小说工作台\server.py` → http://localhost:8899

---

> 本文件由 WORK 模式生成，存于 `docs/方案/`，供其他模式读取。
