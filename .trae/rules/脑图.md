# 小说创作工作台 — 项目脑图

```mermaid
mindmap
  root((小说创作工作台<br/>novel))
    项目规则
      project_rules.md
      顶层规范
      编码约束
      操作流程
    Git版本控制
      小说git操作 SKill
        /提交
        /下载
        /回滚
      远程仓库
        GitHub
        master分支
    AI智能体系统
      主控
        网文主笔-总控
      基础设定
        设定管家-书库
        书风认知-投喂专员
      剧情架构
        爆款解构-剧情分析师
        大纲架构师-章纲
      正文生成
        正文写手-去AI
      灵感创意
        灵感引擎-爽点
      审核优化
        金牌责编-润色
      写作素材
        智能选词词库助手
    Web工作台
      后端 server.py
        端口8899
        REST API
        文件管理
      Web编辑器
        index.html
        app.js
        style.css
      创作看板
      DeepSeek API
    CLI工具
      import_agents.py
      智能体管理
      小说项目管理
      创作看板操作
      工作区文件操作
    tools核心模块
      config.py
      deepseek_api.py
      board_manager.py
      workspace_manager.py
      novels_config.json
   小说项目
     奇幻大陆
       大纲 文稿 设定集
      混在女帝身边的假太监
       大纲 文稿 设定集
       119章节
      异类
       设定集
      素材库
    业务手册
      AI智能体系统
      项目架构
      小说项目管理
```

---

## 项目关系图

```mermaid
flowchart TD
    User[用户] -->|浏览器访问 :8899| Web[Web编辑器]
    Web -->|API请求| Server[server.py]
    Server -->|读写| WM[workspace_manager.py]
    Server -->|读写| BM[board_manager.py]
    Server -->|调用| DS[deepseek_api.py]
    DS -->|HTTP| API[DeepSeek API]
    WM -->|管理| NovelDir[小说项目目录]
    BM -->|管理| Board[创作看板数据]
    Server -->|静态文件| EditorDir[编辑器前端]

    subgraph CLI
        CLI[import_agents.py]
        CLI --> WM
        CLI --> BM
        CLI --> DS
    end

    subgraph Git
        GitSkill[小说git操作 Skill]
        GitSkill -->|提交| GitHub
        GitSkill -->|下载| GitHub
    end

    subgraph Manual
        Rules[project_rules.md] -.->|按需读取| Manuals[docs/手册/]
        Manuals -->|AI智能体.md| AI_Detail
        Manuals -->|项目架构.md| Arch_Detail
        Manuals -->|小说项目管理.md| Novel_Mgmt
    end

    CLI -.->|可选替代| User
    GitSkill -.->|AI代理执行| Novel[项目根目录]
```

---

## 数据流

```mermaid
flowchart LR
    Create[创建/编辑章节] -->|workspace_manager.py| Save[保存到文稿/]
    Save -->|board_manager.py| Update[更新创作看板]
    Save -->|/提交| Push[推送到 GitHub]
    AI[AI智能体对话] -->|deepseek_api.py| DeepSeek[DeepSeek API]
    DeepSeek -->|返回结果| Web
```
